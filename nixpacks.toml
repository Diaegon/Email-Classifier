[phases.setup]
nixPkgs = ["python312", "python312Packages.pip"]

[phases.install]
cmds = ["cd apps/backend && pip install -e ."]

[start]
cmd = "cd apps/backend && uvicorn email_classifier_llm.main:app --host 0.0.0.0 --port $PORT"
